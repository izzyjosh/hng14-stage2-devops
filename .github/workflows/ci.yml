name: Job handling microservices CI

on:
  push:
    branches:
      - main
  pull_request:

env:
  REGISTRY: localhost:5000

jobs:
  #------------------------
  # Lint Job
  #------------------------
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # python linting
      - name: Install flake8
        run: pip install flake8

      - name: Run flake*
        run: flake8 .

      # Js linting
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci

      - name: Run ESLint (frontend)
        run: |
          cd frontend
          npx eslint .

      # Dockerfile linting
      - name: Install hadolint
        run: |
          sudo wget -O /usr/local/bin/hadolint \
          https://github.com/hadolint/hadolint/releases/latest/download/hadolint-Linux-x86_64
          sudo chmod +x /usr/local/bin/hadolint

      - name: Lint Dockerfiles
        run: find . -name "Dockerfile" -exec hadolint {} \;

  #------------------------
  # Test Job
  #------------------------
  test:
    runs-on: ubuntu-latest
    needs: lint

    services:
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: --health-cmd "redis-cli ping" --health-interval 10s --health-timeout 5s --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Run API tests
      - name: Install dependencies
        run: |
          cd api
          pip install -r requirements.txt
          pip install httpx pytest pytest-cov

      - name: Run API tests
        run: |
          cd api
          pytest --cov=. --cov-report=xml

      # Run Worker tests
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml

  #------------------------
  # Build Job
  #------------------------
  build:
    runs-on: ubuntu-latest
    needs: test

    services:
      registry:
        image: registry:2
        ports:
          - 5000:5000

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build and tag images
        run: |
          SHA=${{ github.sha }}

          docker build -t $REGISTRY/api:$SHA -t $REGISTRY/api:latest ./api
          docker build -t $REGISTRY/worker:$SHA -t $REGISTRY/worker:latest ./worker
          docker build -t $REGISTRY/frontend:$SHA -t $REGISTRY/frontend:latest ./frontend

      - name: Push images to registry
        run: |
          SHA=${{ github.sha }}

          docker push $REGISTRY/api:$SHA
          docker push $REGISTRY/api:latest

          docker push $REGISTRY/worker:$SHA
          docker push $REGISTRY/worker:latest

          docker push $REGISTRY/frontend:$SHA
          docker push $REGISTRY/frontend:latest

  #------------------------
  # Security Scan Job
  #------------------------
  security:
    runs-on: ubuntu-latest
    needs: build

    services:
      registry:
        image: registry:2
        ports:
          - 5000:5000

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Trivy
        run: |
          sudo apt-get update
          sudo apt-get install -y curl
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin

      # -----------------------
      # REBUILD IMAGES (CRITICAL FIX)
      # -----------------------
      - name: Build images for scan
        run: |
          docker build -t api:scan ./api
          docker build -t worker:scan ./worker
          docker build -t frontend:scan ./frontend

      # -----------------------
      # SCAN IMAGES
      # -----------------------
      - name: Scan images
        run: |
          FAILED=0

          trivy image --severity CRITICAL --exit-code 1 \
            --format sarif -o trivy-api.sarif api:scan || FAILED=1

          trivy image --severity CRITICAL --exit-code 1 \
            --format sarif -o trivy-worker.sarif worker:scan || FAILED=1

          trivy image --severity CRITICAL --exit-code 1 \
            --format sarif -o trivy-frontend.sarif frontend:scan || FAILED=1

          exit $FAILED

      - name: Upload SARIF
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report
          path: |
            trivy-api.sarif
            trivy-worker.sarif
            trivy-frontend.sarif

  #------------------------
  # Integration Test Job
  #------------------------
  integration:
    runs-on: ubuntu-latest
    needs: security

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose

      - name: Start services with Docker Compose
        run: |
          docker-compose up -d --build

      - name: Wait for services to be healthy
        run: |
          while ! docker ps | grep "healthy"; do
            echo "Waiting for services to be healthy..."
            sleep 5
          done

      - name: Submit job
        run: |
          curl -X POST http://localhost:8000/jobs > response.json

      - name: Poll until done
        run: |
          for i in {1..10}; do
            STATUS=$(curl -s http://localhost:8000/jobs | jq -r '.status')
            if [ "$STATUS" = "completed" ]; then
              exit 0
            fi
            sleep 3
          done
          exit 1

      - name: Cleanup
        if: always()
        run: docker compose down

  #------------------------
  # Deploy Job
  #------------------------
  deploy:
    runs-on: ubuntu-latest
    needs: integration
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # -----------------------
      # REBUILD IMAGES (CRITICAL FIX)
      # -----------------------
      - name: Build images for deploy
        run: |
          docker build -t api:latest ./api
          docker build -t worker:latest ./worker
          docker build -t frontend:latest ./frontend

      # -----------------------
      # API ROLLING DEPLOY
      # -----------------------
      - name: Deploy API (rolling)
        run: |
          set -e

          docker run -d --name api_new -p 8001:8000 api:latest

          for i in {1..12}; do
            if curl -f http://localhost:8001/health; then
              docker rm -f api || true
              docker rename api_new api
              exit 0
            fi
            sleep 5
          done

          docker rm -f api_new
          exit 1

      # -----------------------
      # FRONTEND DEPLOY
      # -----------------------
      - name: Deploy Frontend (rolling)
        run: |
          set -e

          docker run -d --name frontend_new -p 3000:3000 frontend:latest

          for i in {1..12}; do
            if curl -f http://localhost:3000; then
              docker rm -f frontend || true
              docker rename frontend_new frontend
              exit 0
            fi
            sleep 5
          done

          docker rm -f frontend_new
          exit 1

      # -----------------------
      # WORKER DEPLOY
      # -----------------------
      - name: Deploy Worker
        run: |
          set -e

          docker run -d --name worker_new worker:latest

          sleep 10

          if docker ps | grep worker_new; then
            docker rm -f worker || true
            docker rename worker_new worker
          else
            docker rm -f worker_new
            exit 1
          fi